{
  "name": "hpc-jupyter-vscode",
  "description": "Launches a JupyterLab server on a SLURM compute node and tunnels it to VSCode as a remote Jupyter kernel, submitted via sbatch so the session survives terminal/SSH disconnects, with an idle-timeout safety net.",
  "version": "1.0.0",
  "author": {
    "name": "Matt Schechter"
  }
}
